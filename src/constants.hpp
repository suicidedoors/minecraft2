#pragma once
#include <glm/glm.hpp>

const int SCREEN_WIDTH = 1920;
const int SCREEN_HEIGHT = 1080;

const float MOUSE_SENSITIVITY = 0.1f;
const float ARROW_SENSITIVITY = 1.5f;

const glm::vec3 STARTING_CAMERA_POSITION = {0.0f, 10.0f, 3.0f};
